<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>
        <%= title %> | Dink Studio Showroom
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>


</head>

<body class="antialiased">
    <%- include('partials/header') %>

        <main class="max-w-7xl mx-auto px-6 pt-32 pb-24 page-offset">

            <div class="flex flex-col lg:flex-row lg:items-end justify-between mb-24 gap-12">
                <div class="reveal-item">
                    <p class="text-[10px] font-black uppercase tracking-[0.5em] text-[#C5A059] mb-4">Collection 2026</p>
                    <h1 class="text-6xl md:text-8xl font-black tracking-tighter leading-[0.8] mb-8">
                        THE <br><span class="text-[#C5A059]">ARCHIVE.</span>
                    </h1>
                    <p class="text-zinc-500 max-w-sm text-sm font-medium leading-relaxed border-l border-zinc-800 pl-6">
                        Engineering luxury for the modern Ethiopian interior. Every piece is a fusion of UV precision
                        and artisanal soul.
                    </p>
                </div>

                <div class="reveal-item flex items-center gap-4">
                    <div class="glass-card flex items-center p-1 rounded-full px-6 py-2">
                        <span class="text-[9px] font-black uppercase tracking-widest text-zinc-500 mr-4">Sort By</span>
                        <select id="sortSelect"
                            class="bg-transparent text-[10px] font-black uppercase tracking-widest outline-none cursor-pointer pr-8 text-white">
                            <option value="newest">Latest Arrivals</option>
                            <option value="price-asc">Price: Low - High</option>
                            <option value="price-desc">Price: High - Low</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="flex flex-col lg:flex-row gap-16">
                <aside class="w-full lg:w-64 flex-shrink-0">
                    <div class="sticky top-32 space-y-10 reveal-item">
                        <div class="relative group">
                            <input type="text" id="searchInput" placeholder="Search Archive..."
                                class="w-full bg-zinc-900/40 border border-white/5 rounded-full p-5 px-10 text-[10px] font-black uppercase tracking-widest outline-none focus:border-[#C5A059] transition-all placeholder-zinc-700">
                            <div
                                class="absolute right-5 top-1/2 -translate-y-1/2 text-zinc-700 group-focus-within:text-[#C5A059]">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3"
                                        d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                                </svg>
                            </div>
                        </div>

                        <div class="space-y-3" id="category-filters">
                            <h3 class="text-[10px] font-black uppercase tracking-[0.4em] text-zinc-800 mb-6 pl-2">System
                                Categories</h3>
                            <button
                                class="filter-btn active-filter w-full text-left px-6 py-4 rounded-xl text-[10px] font-black uppercase tracking-[0.2em] transition-all"
                                data-category="all">
                                00. All Objects
                            </button>
                            <% categories.forEach((cat, i)=> { %>
                                <button
                                    class="filter-btn w-full text-left px-6 py-4 rounded-xl text-[10px] font-black uppercase tracking-[0.2em] text-zinc-500 hover:text-white border border-transparent hover:border-white/5 transition-all"
                                    data-category="<%= cat %>">
                                    0<%= i + 1 %>. <%= cat %>
                                </button>
                                <% }) %>
                        </div>
                    </div>
                </aside>

                <div class="flex-1">
                    <div id="product-grid" class="grid grid-cols-1 md:grid-cols-2 gap-x-10 gap-y-16">
                        <% products.forEach(product=> { %>
                            <article class="product-card reveal-item group cursor-pointer"
                                data-category="<%= product.category %>" data-name="<%= product.name.toLowerCase() %>"
                                data-price="<%= product.price %>"
                                onclick="window.location.href='/product/<%= product.code %>'">

                                <div
                                    class="glass-card rounded-[2.5rem] aspect-[4/5] relative mb-8 overflow-hidden flex items-center justify-center p-12">
                                    <div
                                        class="absolute top-8 left-8 flex items-center gap-2.5 bg-black/40 backdrop-blur-md px-4 py-2 rounded-full border border-white/10">
                                        <span
                                            class="w-1.5 h-1.5 rounded-full bg-[#C5A059] shadow-[0_0_10px_#C5A059]"></span>
                                        <span class="text-[8px] font-black uppercase tracking-widest text-zinc-300">In
                                            Stock</span>
                                    </div>

                                    <% if(product.image) { %>
                                        <img src="<%= product.image %>" alt="<%= product.name %>" class="product-img">
                                        <% } else { %>
                                            <span class="text-6xl font-black text-zinc-900 uppercase italic">
                                                <%= product.code %>
                                            </span>
                                            <% } %>

                                                <div
                                                    class="absolute inset-0 bg-[#C5A059]/10 opacity-0 group-hover:opacity-100 transition-opacity duration-500 flex items-center justify-center">
                                                    <div
                                                        class="w-16 h-16 bg-white text-black rounded-full flex items-center justify-center translate-y-10 group-hover:translate-y-0 transition-transform duration-500 shadow-2xl">
                                                        <svg class="w-6 h-6" fill="none" stroke="currentColor"
                                                            viewBox="0 0 24 24">
                                                            <path stroke-linecap="round" stroke-linejoin="round"
                                                                stroke-width="2.5" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
                                                        </svg>
                                                    </div>
                                                </div>
                                </div>

                                <div class="px-2 flex justify-between items-end">
                                    <div>
                                        <p class="text-[9px] font-black text-[#C5A059] uppercase tracking-[0.3em] mb-2">
                                            <%= product.category %>
                                        </p>
                                        <h2 class="text-3xl font-black tracking-tighter text-white leading-none">
                                            <%= product.name %>
                                        </h2>
                                    </div>
                                    <div class="text-right">
                                        <p class="text-[9px] font-bold text-zinc-600 uppercase tracking-widest mb-1">
                                            Price Ref.</p>
                                        <p class="text-2xl font-black tracking-tighter text-white">ETB <%=
                                                product.price.toLocaleString() %>
                                        </p>
                                    </div>
                                </div>
                            </article>
                            <% }) %>
                    </div>

                    <div id="empty-state" class="hidden py-40 text-center glass-card rounded-[3rem]">
                        <div class="max-w-xs mx-auto space-y-6">
                            <p class="text-zinc-700 text-5xl font-black">404</p>
                            <h3 class="text-xs font-black text-zinc-500 uppercase tracking-[0.3em]">No Matching
                                Inventory Found</h3>
                            <button onclick="window.location.reload()"
                                class="px-8 py-3 border border-zinc-800 text-[10px] font-black uppercase tracking-widest hover:border-[#C5A059] hover:text-[#C5A059] transition-all">
                                Reset Filters
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <%- include('partials/footer') %>


</body>

</html>